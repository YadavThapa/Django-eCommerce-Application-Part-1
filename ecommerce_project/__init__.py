"""Top-level proxy package for the Django project.

This package wraps the real project under
`main/ecommerce_project` so it can be imported as
`ecommerce_project` from the repository root. Runtime behaviour is kept
unchanged.
"""

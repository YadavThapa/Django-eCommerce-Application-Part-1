"""Legacy root files package.

This package contains archived copies of scripts that used to live at the
project root. We keep a thin shim at root that imports from this package so
legacy imports continue to work.
"""

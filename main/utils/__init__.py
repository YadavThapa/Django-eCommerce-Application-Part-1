"""Minimal utils package restored to avoid import errors for legacy code.

This file intentionally keeps the package empty. If a real utils module
is needed, we should restore its implementation from backups or move the
required helpers into `shop.utils`.
"""

__all__: list[str] = []

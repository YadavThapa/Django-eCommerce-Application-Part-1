#!/usr/bin/env python
"""Deprecated: moved to top-level `tests/` during cleanup.

This file used to live at `main/tests_organized/integration/test_real_functionality.py`.
The canonical copy is now at `tests/integration/test_real_functionality.py`.
A backup of the original file is stored under `archived/backup_20251013_170500/`.

This stub intentionally does nothing and exits to avoid surprising behavior
if someone runs the old path.
"""

import sys

print(
    "[DEPRECATED] integration/test_real_functionality moved to top-level "
    "'tests/integration/' directory"
)
sys.exit(0)

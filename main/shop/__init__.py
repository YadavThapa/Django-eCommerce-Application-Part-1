# Shop app - Flat structure
# All actual files are in the root directory with shop_ prefix

# When the editor runs outside a project's virtualenv it may show
# import-error for Django packages. This file is a small shim; disable
# that lint rule here so the editor doesn't complain when no venv is
# active.
# pylint: disable=import-error
